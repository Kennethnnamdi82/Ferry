services:
  - type: web
    runtime: static
    name: ferry-frontend
    rootDir: ferry
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
